<h1>hello all articles</h1>

<%= link_to "Add article", new_article_path %>
<ul>
  <% @articles.each do |article| %>
  <li>title : <%= link_to article.title, article_path(article) %>, content : <%= article.content %> </li>
  <% end %>
</ul>
